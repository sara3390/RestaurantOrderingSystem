@model IEnumerable<RestaurantOrderingSystem.Models.Table>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
    <tr>
        <th>
            number
        </th>
        <th>
            @Html.DisplayNameFor(model => model.numOfChairs)
        </th>

        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TableID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.numOfChairs)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TableID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TableID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TableID">Delete</a>
            </td>
        </tr>
    }
    </tbody>
</table>